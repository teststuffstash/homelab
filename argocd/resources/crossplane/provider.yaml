# provider-terraform — runs Terraform modules in-cluster as Workspace CRs. Ref confirmed
# resolvable (Installed+Healthy). runtimeConfigRef injects the Garage admin env.
# Digest-pinned (FU-011, 2026-09-03): the digest is the one the running revision resolved from
# the :v1.1.1 tag (read off the pod's imageID). v1.1.1 embeds Terraform 1.5.5; a bump to v1.2.0+
# changes the engine and is its own decision, not a pin refresh.
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-terraform
spec:
  package: xpkg.crossplane.io/crossplane-contrib/provider-terraform:v1.1.1@sha256:ff4f55e5589d1df3de8e210792ed391ed0c84f0892adcd4f31d09d3fba42281d
  runtimeConfigRef:
    name: terraform-runtime
