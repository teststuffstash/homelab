apiVersion: v1
kind: Service
metadata:
  name: cloudflare-edge-probe
  labels: { app: cloudflare-edge-probe }
spec:
  selector: { app: cloudflare-edge-probe }
  ports:
    - { name: http-metrics, port: 9506, targetPort: http-metrics }