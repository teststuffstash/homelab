# ── observation point ── pin the block's observable effect: the resolved model name
# and the verdict string.  The expected/ files pin the exact values.
echo "RESOLVED_MODEL='${RESOLVED_MODEL-}'"
echo "RESOLVED_VERDICT='${RESOLVED_VERDICT-}'"