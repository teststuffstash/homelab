# ── observation point ── not launcher code. In reviewer-session.sh the statement after the
# currency-gate block is the FU-092 pod-label probe / pod create. This line stands in for
# "execution continued past the gate" — its ABSENCE from the action stream is the assertion
# that the fail-open probe did NOT wrongly skip (the goal-budget-refusal observation pattern).
echo "REACHED: past-currency-gate"
