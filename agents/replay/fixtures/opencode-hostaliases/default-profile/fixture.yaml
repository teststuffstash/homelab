# homelab#990 — opencode hostAliases fail-fast: on ENFORCED opencode rides (the gate is
# HARNESS=opencode ∧ EGRESS_ENFORCE=true — monitor-mode gets no stubs, see the monitor-mode row),
# stub models.dev + models.opencode.ai to 127.0.0.1, and registry.npmjs.org when the egress
# profile is NOT "node" (where the CNP denies it anyway — silent-drop → fast-fail).
#
# CONDITION UNDER REPLAY: HARNESS=opencode, EGRESS_PROFILE unset (default/non-node profile).
# THE CONTRACT: HOST_ALIASES is non-empty and carries all three stubs.
name: opencode-hostaliases-default-profile
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:opencode-hostaliases
  - file:post.sh