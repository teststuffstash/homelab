CALL item_class_push homelab issue-299 guarded-path operator
CALL item_class_push homelab issue-400 guarded-path operator
CALL item_class_push homelab issue-405 guarded-path operator
OUT DISPATCH #309 (homelab, declared: agents/coordinator-scan.sh, agents/replay/)
OUT DISPATCH #401 (homelab, declared: argocd/resources/loki/)
OUT DISPATCH #402 (homelab, declared: *)
OUT DISPATCH #403 (homelab, declared: **/*.yaml)
OUT DISPATCH #404 (oracle-fleet, declared: agents/coordinator/)
OUT ORPHAN [homelab] ⛔ pin-only GUARDED path — NOT dispatched (a PR may write only a pin line there, so `ci` is structurally red; the route is an operator push to master — CODEOWNERS §Carve-outs). Re-scope or split the issue, or hand it to the operator:
OUT ORPHAN   issue #299 — goal child: the OpenRouterKey manifest + the SCOUT_MCP_KEY env line (declared: agents/coordinator/ → guarded: agents/coordinator/reflexes-argo.yaml)
OUT ORPHAN [homelab] ⛔ pin-only GUARDED path — NOT dispatched (a PR may write only a pin line there, so `ci` is structurally red; the route is an operator push to master — CODEOWNERS §Carve-outs). Re-scope or split the issue, or hand it to the operator:
OUT ORPHAN   issue #400 — openrouter-operator: add a syncPolicy retry backoff (declared: argocd/platform/openrouter-operator.yaml → guarded: argocd/platform/openrouter-operator.yaml)
OUT ORPHAN [homelab] ⛔ pin-only GUARDED path — NOT dispatched (a PR may write only a pin line there, so `ci` is structurally red; the route is an operator push to master — CODEOWNERS §Carve-outs). Re-scope or split the issue, or hand it to the operator:
OUT ORPHAN   issue #405 — split the platform app-of-apps directory (declared: argocd/platform/ → guarded: argocd/platform/arc-runners.yaml argocd/platform/openrouter-operator.yaml)
OUT REACHED: end
RC 0
