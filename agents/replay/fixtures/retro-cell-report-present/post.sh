# ── observation point ── not cell code; the same stand-in the dead-cell sibling uses. Here it is
# PRESENT, which is the half of the pair that says the check lets a good ride through.
echo "REACHED: cell exits 0 — the DAG marks this cell Succeeded"
