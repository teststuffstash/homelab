# ── observation point ── not launcher code. The region's whole product is the pair (BASE_REF,
# NO_ARM): what the pod forks from, and whether the PR arms. Printing both is what makes the
# block/legacy twins comparable at all — the stdout line alone would go green on a reader that
# announced a base it then failed to assign.
echo "BASE_REF=${BASE_REF} NO_ARM=${NO_ARM:-<empty>}"
# Never reached on the refusal row; its absence there is that row's assertion.
echo "REACHED: end"
