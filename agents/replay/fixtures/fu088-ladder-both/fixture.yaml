# fu088-ladder-both (homelab#439 leg 2) — the fu088-gates `*)` arm, BOTH-LATCHED leg.
#
# CONDITION UNDER REPLAY: both rails latched — the ladder exits 1 with empty stdout.
#
# THE CONTRACT: the spawn defers report-only (exit 0, the deferred line naming both rails) — the
# same burn-saver as the pre-leg-2 Anthropic-only defer, just renamed to name the whole ladder.
name: fu088-ladder-both
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:fu088-gates
env:
  - STUB_RAIL=
  - STUB_RAIL_RC=1
