# ── observation point ── not cell code; the same stand-in the present/dead-cell siblings use.
# Here the multi-block log has a real report as its LAST block, so the check must let the cell
# through — the empty skeletons and inter-block text must be dropped, not cause a failure.
echo "REACHED: cell exits 0 — the DAG marks this cell Succeeded"