# ── observation point ── not cell code. Its ABSENCE from the action stream is the assertion: a
# skeleton-only report must not let the cell reach its Succeeded exit any more than a missing one
# does — the whole point of the floor is that "markers present" alone is not "report present".
echo "REACHED: cell exits 0 — the DAG marks this cell Succeeded"
