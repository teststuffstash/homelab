# ── bridge — seams for testing the extracted touches-check helper (fp_conflict_multi).

HERE="$REPLAY_ROOT/agents"

# Source the extracted helper (same logic the scan uses)
. "${HERE}/touches-check.sh"

export HERE
