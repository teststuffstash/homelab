# end-state-not-proven: the re-read after both writes still shows `agent/in-progress` — the
# remove did not take. Exit 5, not a refusal: the writes were issued and a human looks.
. + [{ "name": "agent/in-progress" }]
