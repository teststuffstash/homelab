# ── drive ── one unit per leg, straight through the real fast path.
echo "REACHED: human-authored PR (the jail seat) — settles with no mandate, before any spend"
fast_unit_dispatch "changes-requested|circles|pr-396"
printf 'RETURN %s\n' "$?"

echo "REACHED: end"
