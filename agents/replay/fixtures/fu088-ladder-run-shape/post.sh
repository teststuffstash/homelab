# ── observation point ── not launcher code. The finished pod command is the observable: for the
# `--run` shape the round-1 failure mode was a NO-OP substitution (command unchanged) with a
# success log on top — this line pins the command, so a no-op substitution reds the fixture.
echo "RUN_CMD: ${RUN_CMD}"
