# ── drive ── one unit, straight through the real fast path.
echo "REACHED: assembly PR with goal/** head — settles with FU-143 exclusion, before any spend"
fast_unit_dispatch "changes-requested|oracle-fleet|pr-309"
printf 'RETURN %s\n' "$?"

echo "REACHED: end"