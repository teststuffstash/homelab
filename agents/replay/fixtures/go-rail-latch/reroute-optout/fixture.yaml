# The FU-088 gate's Go arm, CAPACITY-limited + subscriptionFallback OPT-OUT leg (homelab#600).
#
# CONDITION UNDER REPLAY: an opencode-go/* dispatch while the Go window is capacity-latched
# (reason "window-weekly"), task is an issue-fix ride, but the stack row sets
# `subscriptionFallback: false` (and AGENT_SUBSCRIPTION_FALLBACK is defaulted to 1).
#
# THE CONTRACT: the M12 opt-out binds on the reroute path too — the stack that says "strictly
# wait" is not moved to the subscription. The ride is left to the gates below exactly as before
# the trigger existed (the strict-wait line, M12 :435 shape). No RAIL_DEGRADED, no MODEL change,
# no RUN_CMD re-point, no rail=subscription-fallback.
name: go-rail-latch-reroute-optout
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:fu088-gates
  - file:post.sh
