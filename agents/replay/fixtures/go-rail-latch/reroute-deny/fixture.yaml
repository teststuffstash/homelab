# The FU-088 gate's Go arm, CAPACITY-limited + modelDeny leg (homelab#600).
#
# CONDITION UNDER REPLAY: an opencode-go/* dispatch while the Go window is capacity-latched
# (reason "window-weekly"), task is an issue-fix ride, but the stack's modelDeny lists the
# fallback model (claude/haiku).
#
# THE CONTRACT: the M12 modelDeny binds on the reroute path too — a degrade is still a MODEL
# CHOICE, and a capacity condition is not a reason to hand a stack a model it has explicitly
# refused. The claim wins over the fallback: strict wait, no RAIL_DEGRADED, no MODEL change.
name: go-rail-latch-reroute-deny
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:fu088-gates
  - file:post.sh
