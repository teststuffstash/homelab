# The FU-088 gate's Go arm, CAPACITY-limited + UN-THREADABLE → DEFER leg (homelab#600).
#
# CONDITION UNDER REPLAY: an opencode-go/* dispatch while the Go window is capacity-latched
# (reason "window-weekly"), task is an issue-fix ride, but the pod command is NEITHER the
# recipe/worker shape (no `--model <id> ` token to swap in place) NOR the `claude -p ` --run
# shape — it cannot carry the subscription fallback's model by construction.
#
# THE CONTRACT (finding-2 ratchet, #439, transposed): the reroute DEFERS (exit 0, report-only,
# naming why) instead of dispatching a pod that announces rail=subscription-fallback but serves
# the latched Go rail. The fail-safe is the prior behaviour (a clean defer), never a mis-served
# pod.
name: go-rail-latch-reroute-unthreadable
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:fu088-gates
  - file:post.sh
