# PR#643 r1 (the #629 sibling): the `*)` failover arm — Anthropic latched, Go rail picked —
# re-points MODEL at the rail and must move GOOSE_MODEL with it. Before the fix GOOSE_MODEL
# stayed at its pre-failover value ("haiku") while MODEL/RUN_CMD moved to the Go id, feeding the
# pod-manifest divergence #629's report documents (strike comments naming the fallback while the
# ride served the rail). Same block, same observation shape as ../reroute-model-mismatch.
name: go-rail-latch-failover-model-sync
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:fu088-gates
  - file:post.sh
