# homelab#587 leg 2, sibling of ../set: the launcher's environment does NOT carry RETRO_GH_TOKEN
# (a fixer-namespace ride, or a retro-git secret not yet materialized — retro-argo.yaml's WARN
# path). The fragment must stay empty, so the manifest never splices in an env entry naming an
# unset variable.
name: retro-gh-token-env-unset
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:retro-gh-token-env
  - file:post.sh
