# major-not-armed: #90 carries the lane marker `major` — C9 must leave it alone; the witness #91
# is still armed (the belt itself keeps working).
map(if .number == 90 then .labels += [{"name": "major"}] else . end)
