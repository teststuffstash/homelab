# The Go-rail sibling of harness-run-cmd-claude (ADR-107, platform Go-flash dogfood 2026-08-17).
#
# CONDITION UNDER REPLAY: a recipe dispatch whose model is an opencode-go/* id — the claude
# harness serving the OpenCode Go subscription rail through the egress proxy.
#
# THE CONTRACT: the FULL id survives onto the CLI (`--model opencode-go/deepseek-v4-flash`).
# The pre-fix code collapsed EVERY slash-id to haiku here, so a Go dispatch silently rode the
# Anthropic subscription as haiku while agent_run said flash — the PR#407 fused-semantics class,
# which this stream is what makes red.
name: harness-run-cmd-go
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:harness-run-cmd
  - file:../harness-run-cmd-claude/post.sh
