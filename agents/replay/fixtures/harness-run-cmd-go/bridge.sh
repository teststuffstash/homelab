# ── bridge ── the claude sibling's four variables with the ONE delta under test: MODEL is the
# full opencode-go/* id the model_id parse now leaves UNCHANGED (rail keyed on the prefix by the
# proxy). Everything else identical — the arm must treat a Go ride as an ordinary claude ride.
CTX_PRELUDE=""
RECIPE_B64="UkVDSVBF"
ISSUE_N="19"
HARNESS="claude"
MODEL="opencode-go/deepseek-v4-flash"
RUN_CMD=""
