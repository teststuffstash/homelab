# ── bridge ── raw `openall` (the shape `gh issue list --json number,title,labels,...` returns)
# for the `queued-derivation` block: ONE open issue carrying `agent/queued` and NO `agent-fix` —
# the exact state board.sh's retired "half-labeled" anti-pattern line used to flag, and the
# pre-#1432 JOIN would have EXCLUDED from `queued` entirely.
openall='[{"number":501,"title":"agent/queued-only smoke (#1432)","labels":[{"name":"agent/queued"}]}]'
