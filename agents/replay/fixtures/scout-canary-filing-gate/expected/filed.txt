CALL scout_canary_mint model=acme/foo:free free=true harness=opencode
CALL scout_canary_ride model=acme/foo:free harness=opencode attempt=1
CALL scout_canary_mint model=acme/foo:free harness=opencode cleanup
CALL scout_canary_mint model=bigco/bar free=false harness=opencode
CALL scout_canary_ride model=bigco/bar harness=opencode attempt=1
CALL scout_canary_mint model=bigco/bar harness=opencode cleanup
CALL rotation_post source=canary entry={"model":"acme/foo:free","harness":"opencode","provider":"","attempt":1,"canary_verdict":"clean","free":true}
CALL rotation_post source=canary entry={"model":"bigco/bar","harness":"opencode","provider":"","attempt":1,"canary_verdict":"clean","free":false}
CALL gh issue create --repo teststuffstash/homelab --title 🔭 model scout: 2 new candidate model(s) (<today>) --body Weekly model scout (REPORT-ONLY, FU-062 / docs/agents/model-routing.md §M7): models whose BASE id is NEW on OpenRouter since the last tick, advertise `tools`, and are `:free` or ≤ $0.50/M headline. Ranked free-first, then by AA agentic/coding — the order canary slots are spent in.\n\n| # | model | AA int/code/agentic | effective $/M in | price note | pinned provider | uptime | providers | canary |\n|---|---|---|---|---|---|---|---|---|\n| 1 | `acme/foo:free` | 42 / 40 / 38 | $0 | free tier | `acme` | 99.7% | 1 | opencode=clean |\n| 2 | `bigco/bar` | `unbenched` | — | — | `—` | — | — | opencode=clean |\n\n*effective $/M = cache-aware per-provider min at 80% cache hit (§M3); pinned provider = the tools-capable session pin `--lookup` would choose (§M4). AA indices = MCP `get-model` (§M7 leg 2); `unbenched` = not in the Artificial-Analysis feed, which is the normal state of a genuine newcomer — the canary rung, not the benchmark, is what speaks for those. `canary` = the rung-1 rail-probe verdict (§M7 leg 3), typed (`error_class` vocabulary); `—` = not canaried this tick.*\n\n**Graduation is a human call**: add worthy entries to `agents/stacks.json` `workerModelFallbacks` — evidence, not vibes. The same commit MUST add the model_tiers line (router-self-test enforces chain ⊆ tiers since 2026-08-03) — ready to paste:\n\n`"acme/foo:free": "free"` (argocd/resources/openrouter-proxy/model-classes.json — merging rolls the proxy)\n`"bigco/bar": "?"` (argocd/resources/openrouter-proxy/model-classes.json — merging rolls the proxy)\n\n**Canary rides** (FU-062/FU-024/FU-161 + ADR-112 cells — one row per ATTEMPT: (model × harness × provider-arm); `proxy pin` = the M4 default, a named arm = the `@` provider_slot/slug pin with fallbacks OFF; `only-free` = proxy 403s any paid model pre-spend):\n\n| model | harness | provider arm | attempt | verdict |\n|---|---|---|---|---|\n| `acme/foo:free` | opencode | proxy pin | 1 | clean (only-free) |\n| `bigco/bar` | opencode | proxy pin | 1 | clean ($0.05 cap) |\n\n*A TYPED verdict carries the launcher's `error_class`, never a bare `failed`: `clean` = the model completed a real tool-calling ride on a budget-capped key; `auth-storm`/`timeout`/`harness-death`/`budget-403`/… = the rail or the key failed, typed; `suspect-infra`/`inconclusive` = a benchmark-capable model that fails the rail (contradiction rule, leg 4). A canary verdict is evidence for graduation, not automatic graduation. Full outcome + transcript in the ledger (model label).*
OUT ROW 1 acme/foo:free benched=true canary=opencode=clean
OUT ROW 2 bigco/bar benched=false canary=opencode=clean
OUT CANARY acme/foo:free clean free=true
OUT CANARY bigco/bar clean free=false
ERR canary: riding up to 3 candidate(s) in ns teststuffstash
ERR canary: dispatching acme/foo:free harness=opencode arm=<proxy-pin> attempt=1 (only-free)
ERR canary: dispatching bigco/bar harness=opencode arm=<proxy-pin> attempt=1 ($0.05 cap)
ERR → posting digest issue on teststuffstash/homelab
RC 0
