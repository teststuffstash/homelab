# homelab#1175 round 2 — the env card issue-context line gates on TASK matching issue-[0-9]*.
# This arm: TASK=issue-1175 ⇒ the "Issue context" line IS emitted.
name: env-card-issue-context-gate-issue-task
mode: actions
source: agents/agent-session.sh
parts:
  - block:render_env_card
  - file:pre.sh
  - file:post.sh