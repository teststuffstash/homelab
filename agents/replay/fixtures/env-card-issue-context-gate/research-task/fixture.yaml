# homelab#1175 round 2 — the env card issue-context line gates on TASK matching issue-[0-9]*.
# This arm: TASK=research-1175-slug, ISSUE_N=1175 (the exact live shape for research fan-out).
# The "Issue context" line MUST NOT be emitted — research tasks never get /work/issue.md written.
name: env-card-issue-context-gate-research-task
mode: actions
source: agents/agent-session.sh
parts:
  - block:render_env_card
  - file:pre.sh
  - file:post.sh