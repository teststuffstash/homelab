# homelab#1175 round 3 — single fixture dir with two arms in one action stream.
#   - arm 1: TASK=issue-1175 ⇒ the "Issue context" line IS emitted
#   - arm 2: TASK=research-1175-fanout, ISSUE_N=1175 ⇒ it is NOT emitted
#
# Collapsed from three dirs (parent + issue-task + research-task) because the ADR-103
# pin-vacuity gate (homelab#1107) runs each changed fixture dir against the BASE tree,
# and a negative-only arm cannot red there: the Issue-context line is new in this PR.
name: env-card-issue-context-gate
mode: actions
source: agents/agent-session.sh
parts:
  - block:render_env_card
  - file:pre.sh
  - file:post.sh