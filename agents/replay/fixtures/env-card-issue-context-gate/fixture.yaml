# homelab#1175 round 2 — the env card issue-context line gates on TASK matching issue-[0-9]*
# (same predicate the prefetch block uses), NOT on ISSUE_N alone. Two arms:
#   - issue-task: TASK=issue-1175 ⇒ the "Issue context" line IS emitted
#   - research-task: TASK=research-1175-slug, ISSUE_N=1175 ⇒ it is NOT emitted
#
# CONDITION UNDER REPLAY. render_env_card() with no TASK or ISSUE_N set (same as the existing
# env-card-* fixtures — output unchanged by this edit).
name: env-card-issue-context-gate
mode: actions
source: agents/agent-session.sh
parts:
  - block:render_env_card
  - file:pre.sh
  - file:post.sh