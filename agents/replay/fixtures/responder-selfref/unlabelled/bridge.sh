# ── bridge ── byte-for-byte the sibling's world with ONE thing removed: the
# `platform_machinery: "true"` label. This is the #237 payload exactly as it arrived on
# 2026-08-11, and the gate must read it exactly as it did then — dispatchable.
NAME="RetroReportOverdue"
FP="5d87a09c332779c8"
_ns=""
a='{"status":"firing","fingerprint":"5d87a09c332779c8","labels":{"alertname":"RetroReportOverdue","severity":"warning"}}'
