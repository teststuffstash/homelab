# ── observation point ── not launcher code. MCP_PREP is an internal variable; this prints its
# content so the action stream asserts the rendered commands. The base64 blob is the expected
# mcp_servers JSON for the oracle stack's endpoint and tools.
echo "MCP_PREP: ${MCP_PREP}"