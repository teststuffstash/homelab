# ── observation point ── not launcher code. The CLI's framed output has been stripped to its
# brace span and wrapped in the claude --output-format json envelope, so one downstream parse
# serves both invocation paths.
echo "claude_reply: ${claude_reply:-}"
