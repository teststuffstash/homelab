# homelab#923 — re-review.sh --model flag: pickle shadow re-reviews.
#
# CONDITION UNDER REPLAY. A shadow re-review invocation with --model opencode-go/kimi-k3:
# the model id is passed through verbatim to claude, no verdict-posting call is made,
# and the report is written to stdout with a shadow-re-review marker.
#
# The id was `opencode/big-pickle` until 2026-09-18 (homelab#946): a ZEN id now takes the
# opencode-CLI arm — pinned by the `re-review-shadow-zen` sibling — because the vendor admits no
# API client to its free tier. A Go id keeps this fixture on the claude arm, which is what it
# was always pinning: verbatim pass-through of a rail-prefixed id.
#
# THE CONTRACT:
#   1. The model id is passed through verbatim to claude (--model opencode-go/kimi-k3).
#   2. No verdict-posting call (gh pr comment) is in the composed command.
#   3. The shadow-re-review marker is present in the output.
#   4. The rail is attributed in the report header.
name: harness-run-cmd-re-review-shadow
mode: actions
source: agents/re-review.sh
parts:
  - file:bridge.sh
  - block:re-review-shadow
  - file:post.sh