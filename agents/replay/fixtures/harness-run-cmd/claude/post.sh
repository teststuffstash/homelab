# ── observation point ── not launcher code. See the goose sibling's post.sh: the finished pod
# command is the observable, and printing it is what puts "no GOOSE_MAX_TOKENS here" into the
# asserted stream instead of leaving it to prose.
echo "RUN_CMD: ${RUN_CMD}"
