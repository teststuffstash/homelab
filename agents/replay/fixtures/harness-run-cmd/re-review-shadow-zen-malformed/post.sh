# ── observation point ── not launcher code. The shadow re-review report is written to stdout
# with a shadow-re-review marker. The claude invocation command is captured in claude_reply.
# No verdict-posting call (gh pr comment) is made in shadow mode.
echo "claude_reply: ${claude_reply:-}"