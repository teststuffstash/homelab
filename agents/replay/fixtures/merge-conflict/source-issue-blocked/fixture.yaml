# merge-conflict/source-issue-blocked (homelab#1444) — the merge-conflict clause's
# agent/blocked source-issue hold, pinned.
#
# CONDITION UNDER REPLAY: A worker-authored merge-conflict PR (#700) references issue #1410
# in its body (Fixes #1410). Issue #1410 is marked with agent/blocked label. The scan's
# merge-conflict clause should HOLD the PR and emit a report line, not dispatch a unit.
#
# THE CONTRACT (acceptance item 1):
#   1. A merge-conflict unit is not emitted while its source issue is agent/blocked
#   2. The PR appears as a report line in orphans
#   3. No gh pr view call is made to fetch state-fp data
#
pins: []
name: merge-conflict-source-issue-blocked
mode: actions
source: agents/coordinator-scan.sh
parts:
  - block:state-fp-jq
  - block:state-fp-pair
  - file:bridge.sh
  - block:merge-conflict-gate
  - file:post.sh
env:
  - IN_SLUG=teststuffstash/homelab
  - IN_REPO=homelab
