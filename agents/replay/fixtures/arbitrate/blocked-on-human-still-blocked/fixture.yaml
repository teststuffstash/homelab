# CONDITION UNDER REPLAY. oracle-fleet PR #234 carries `agent/arbitrate` and has a terminal ruling
# that wrote `blocked-on: human` — the ruling determined a human must act. No new human review or
# comment has appeared since the marker. The contract is that the clause reports "BLOCKED-ON"
# instead of dispatching a unit: the blocked-on predicate (homelab#1188) suppresses re-dispatch
# while the named blocker is still unresolved.
name: arbitrate-blocked-on-human-still-blocked
mode: actions
source: agents/coordinator-scan.sh
parts:
  - block:blocked-on-jq
  - block:blocked-on-check
  - block:state-fp-jq
  - block:state-fp-pair
  - file:bridge.sh
  - block:arbitrate-gate
  - file:post.sh
env:
  - IN_SLUG=teststuffstash/oracle-fleet
  - IN_REPO=oracle-fleet