# ── bridge ── the launcher variables the guard reads, as a homelab queued-dispatch ride. `PF_SLUG`
# is the repo slug the pre-flight derives from REPO_URL; `WORK_BRANCH` starts empty (a fresh round —
# each arm overrides it); `PF_ISSUE` is the per-arm entry point into the recorded world. The read
# goes through the PATH-shim `gh`, which serves world/gh/pr-list.json and EVALUATES the guard's
# own `--jq` expression — so the mention-vs-strong-link decision is exercised, not transcribed.
PF_SLUG="teststuffstash/homelab"
PF_ISSUE="344"
WORK_BRANCH=""
