# ── bridge ── the same invocation as the rest of the family; only the recorded world differs.
. "$REPLAY_ROOT/agents/machine-comment.sh"

ORG=teststuffstash
PROJECT=circles
GOAL_PARENT=29
GB_SUM=1.75
GB_BUDGET=1
GB_ROWS='    #35 → $0.75 (actual spend, ledger)\n    #36 → $1.00 (cap reservation, live key)\n'
