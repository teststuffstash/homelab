# ── bridge ── same variables as retro-key-minted's bridge, one axis moved: the harness. No
# `python3` seam is declared on purpose — if this branch ever reaches the estimator, the fixture
# should red on a live registry fetch rather than quietly replay one.
HERE="$REPLAY_ROOT/agents"
HARNESS="claude"
MODEL="opus"
PROJECT="oracle-fleet"
RUN_ID="r4"
REVIEW=""
BRIEF="$REPLAY_FIXTURE/brief.md"
KUBECTL="kubectl"
KUBE=""
