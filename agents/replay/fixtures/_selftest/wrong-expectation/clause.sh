# A stand-in clause with a completely known action stream — deliberately NOT extracted from the
# scan, because this fixture tests the HARNESS, and a self-test that also depends on the code under
# test cannot tell you which of the two broke.
echo "UNIT arbitrate|demo|pr-1"
echo "REACHED: end"
