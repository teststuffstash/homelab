# changes-requested/reviewable-again-held (homelab#975) — test re-review hold and blocked-on gate
#
# CONDITION UNDER REPLAY. Tests two paths:
# - PR #1096 fails the reviewable_again hold (newest commit newer than newest review).
# - PR #1099 passes the jq selector but carries a `blocked-on: human` marker, testing the new
#   `pr_blocked_on_check` gate (homelab#1427).
#
# The world carries TWO non-merge commits (round 1 aaaaaaaa1111, round 2 cccccccc1111), both older
# than any merge commit. This pins the oid-selection behaviour: the probe MUST pick the NEWEST
# (cccccccc1111, via sort_by(.committedDate) | last) rather than index 0 (aaaaaaaa1111).
#
# THE CONTRACT:
#   1. For PR #1096: A `gh pr view` call; no UNIT; an ORPHAN line for the re-review hold.
#   2. For PR #1099: A `gh pr view` call; no UNIT; an ORPHAN line for the blocked-on hold.
pins:
  - MP-T11
name: changes-requested-reviewable-again-held
mode: actions
source: agents/coordinator-scan.sh
parts:
  - block:blocked-on-jq
  - block:blocked-on-check
  - block:state-fp-jq
  - block:state-fp-pair
  - file:bridge.sh
  - block:changes-requested-gate
  - file:post.sh
env:
  - IN_SLUG=teststuffstash/homelab
  - IN_REPO=homelab