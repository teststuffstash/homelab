# changes-requested clause with a `blocked-on: human` marker — still unresolved
# (homelab#1427)
#
# CONDITION UNDER REPLAY. PR #456 has reviewDecision=CHANGES_REQUESTED and has a terminal
# ruling that wrote `blocked-on: human` on the PR comment. No human comment or review has
# appeared since the marker was written. The blocked-on predicate must suppress dispatch:
# the clause reports "BLOCKED-ON" instead of emitting a unit.
#
# THE CONTRACT:
#   1. `pr_blocked_on_check` is called with the fetched PR JSON.
#   2. No UNIT is emitted.
#   3. One ORPHAN line is produced reporting the blocked state.
name: changes-requested-blocked-on-human-still-blocked
mode: actions
source: agents/coordinator-scan.sh
parts:
  - block:blocked-on-jq
  - block:blocked-on-check
  - block:state-fp-jq
  - block:state-fp-pair
  - block:round-evidence
  - file:bridge.sh
  - block:changes-requested-gate
  - file:post.sh
env:
  - IN_SLUG=teststuffstash/homelab
  - IN_REPO=homelab
