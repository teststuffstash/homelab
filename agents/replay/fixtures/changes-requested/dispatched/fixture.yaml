# changes-requested/dispatched (homelab#1097) — test normal dispatch and blocked-on gate
#
# CONDITION UNDER REPLAY. Tests two paths:
# - PR #1095 passes the jq selector, all holds, and the no-op probe; dispatches normally.
# - PR #1098 passes the jq selector but carries a `blocked-on: human` marker, testing the new
#   `pr_blocked_on_check` gate (homelab#1427).
#
# THE CONTRACT:
#   1. For PR #1095: A `gh pr view` call; a UNIT is emitted; no ORPHAN line.
#   2. For PR #1098: A `gh pr view` call; no UNIT; an ORPHAN line reporting the blocked-on hold.
pins:
  - MP-T11
name: changes-requested-dispatched
mode: actions
source: agents/coordinator-scan.sh
parts:
  - block:blocked-on-jq
  - block:blocked-on-check
  - block:state-fp-jq
  - block:state-fp-pair
  - block:round-evidence
  - file:bridge.sh
  - block:changes-requested-gate
  - file:post.sh
env:
  - IN_SLUG=teststuffstash/homelab
  - IN_REPO=homelab