# changes-requested clause with a `blocked-on: human` marker — resolved
# (homelab#1427)
#
# CONDITION UNDER REPLAY. PR #457 has reviewDecision=CHANGES_REQUESTED and has a terminal
# ruling that wrote `blocked-on: human` on the PR. A human comment has appeared after the
# marker was written, which resolves the block. The blocked-on predicate must clear: the
# clause proceeds to evaluate other conditions and emits a unit if it reaches dispatch.
#
# THE CONTRACT:
#   1. `pr_blocked_on_check` is called with the fetched PR JSON.
#   2. The check returns "clear", allowing the clause to continue.
#   3. One UNIT is emitted (since this PR meets other dispatch conditions).
name: changes-requested-blocked-on-human-resolved
mode: actions
source: agents/coordinator-scan.sh
parts:
  - block:blocked-on-jq
  - block:blocked-on-check
  - block:state-fp-jq
  - block:state-fp-pair
  - block:round-evidence
  - file:bridge.sh
  - block:changes-requested-gate
  - file:post.sh
env:
  - IN_SLUG=teststuffstash/homelab
  - IN_REPO=homelab
