# ── the session ── NOT clause code, and deliberately not extracted: what sits between the two
# blocks in responder-argo.yaml is `claude -p …`, an LLM call, and whether it reopened anything is
# exactly the thing the strip must not have to TRUST. The world recordings either side of this line
# are the observable consequence — homelab#103 comes back OPEN, homelab#77 stays closed — which is
# the whole reason the guard is a before/after comparison rather than a prompt instruction (ADR-094).
echo "-- session (stubbed): reopened teststuffstash/homelab#103 on the FU-133 subject belt"
