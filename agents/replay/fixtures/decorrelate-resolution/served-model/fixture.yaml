# decorrelate-resolution-served-model — the proxy returns a valid report with served_model.
# decorrelate_arg is set to "--decorrelate-from gpt-4o".
name: decorrelate-resolution-served-model
mode: actions
source: agents/review-reflex.sh
parts:
  - file:bridge.sh
  - block:decorrelate-resolution
  - file:post.sh
scrub:
  - s/[0-2][0-9]:[0-5][0-9]:[0-5][0-9]/HH:MM:SS/g