# decorrelate-resolution-empty-report — the proxy returns an empty report.
# decorrelate_arg stays empty, log says "not available".
name: decorrelate-resolution-empty-report
mode: actions
source: agents/review-reflex.sh
parts:
  - file:bridge.sh
  - block:decorrelate-resolution
  - file:post.sh
scrub:
  - s/[0-2][0-9]:[0-5][0-9]:[0-5][0-9]/HH:MM:SS/g