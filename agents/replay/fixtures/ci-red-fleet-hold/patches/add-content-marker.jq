# The NEGATIVE: the sibling marker names the same <job>/<step> but class=content — the red is the
# PR's own code, so the arm must NOT hold (only infra|environment are fleet classes).
. + [ { "number": 1546,
        "comments": [ { "createdAt": "2026-09-09T12:00:00Z",
                        "body": "ci-cause: ci/OpenTofu-formatting-(fmt--check,-all-roots) class=content basis=observed\n\nThis PR's own diff is unformatted." } ] } ]
