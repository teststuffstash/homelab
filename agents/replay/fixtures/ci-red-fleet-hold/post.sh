# ── observation point ── not scan code. The products the ci-red clause branches on: the orphans
# line the hold emits (or does not), the unit the dispatch leg emits (or does not), and the board
# row the hold pushes. Close the for loop opened in bridge.sh.
done
printf 'ORPHANS %s\n' "$orphans"
printf 'UNITS %s\n' "$units"
printf 'ITEMS %s\n' "$items"
printf 'ITEM_CLASS %s\n' "$ITEM_CLASS_ROWS"
echo "REACHED: end"