# ── bridge ── the three variables the block reads, as agent-session.sh sets them upstream:
# the claim-read egress knob, the baked default harness, and the explicit-flag marker (unset).
EGRESS_ENFORCE="true"
HARNESS="opencode"
unset HARNESS_SET
