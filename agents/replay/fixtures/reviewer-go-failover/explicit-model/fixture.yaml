# reviewer-go-failover-explicit-model (homelab#424) — explicit --model prevents failover.
#
# CONDITION UNDER REPLAY. The /route call (above this block in the shipped script) resolved the
# model decision: the router dispatched with a subscription model. But MODEL_SET_EXPLICIT=1
# (operator passed --model=opus), so the ADR-096 override rule kept opus. _router_defer is
# unset, so this block passes through silently.
#
# THE CONTRACT (executes the REAL gate block from reviewer-session.sh via sentinel):
#   1. With _router_defer unset, the gate does nothing (the model was already set by the route call,
#      with the explicit --model override winning).
#   2. The action stream is empty — no curl calls, no deferral exit.
#
# Parts: bridge.sh (seams: HERE, MODEL_SET_EXPLICIT, _router_defer) + block:reviewer-go-failover-gate
name: reviewer-go-failover-explicit-model
mode: actions
source: agents/reviewer-session.sh
parts:
  - file:bridge.sh
  - block:reviewer-go-failover-gate