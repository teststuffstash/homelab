# reviewer-go-failover-available (homelab#424, G-A 7) — authoritative path: router dispatched Go model.
#
# CONDITION UNDER REPLAY. The /route call (above this block in the shipped script) resolved the
# model decision: since the subscription was latched but the Go rail had capacity, the router
# dispatched with a Go-rail model. _router_adopted=1, so this block passes through silently.
#
# THE CONTRACT (executes the REAL gate block from reviewer-session.sh via sentinel):
#   1. _router_adopted is set, _router_defer is unset → neither arm fires, block passes through.
#   2. The action stream is empty — no curl calls, no deferral exit.
#
# Parts: bridge.sh (seams: HERE, _router_adopted) + block:reviewer-go-failover-gate (real code)
name: reviewer-go-failover-available
mode: actions
source: agents/reviewer-session.sh
parts:
  - file:bridge.sh
  - block:reviewer-go-failover-gate