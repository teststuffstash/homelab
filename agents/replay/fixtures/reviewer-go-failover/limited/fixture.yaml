# reviewer-go-failover-limited (homelab#424) — both rails are limited, defer.
#
# CONDITION UNDER REPLAY. The /route call (above this block in the shipped script) resolved the
# model decision: the router returned a deferral because both subscription and Go rails are
# limited. _router_defer=1 and _rwhy carry the typed reason. This block prints the deferral
# message and exits 0.
#
# THE CONTRACT (executes the REAL gate block from reviewer-session.sh via sentinel):
#   1. _router_defer is set → the gate prints the deferral message and exits 0.
#   2. No curl calls (the route resolution happened above this block).
#
# Parts: bridge.sh (seams: HERE, _router_defer, _rwhy) + block:reviewer-go-failover-gate (real code)
name: reviewer-go-failover-limited
mode: actions
source: agents/reviewer-session.sh
parts:
  - file:bridge.sh
  - block:reviewer-go-failover-gate