# reviewer-go-failover-limited (homelab#424) — both Anthropic AND Go rail are limited.
#
# CONDITION UNDER REPLAY. The subscription-latch.sh probe returns exit 1 (Anthropic limited),
# AND the Go rail probe (/opencode-limit) returns {limited: true}.
#
# THE CONTRACT:
#   1. When Go is also limited, the gate DEFERS (exit 0, no pod created).
#   2. The log line "→ review of ... deferred — subscription rate-limited (FU-088 latch)" is emitted.
#   3. MODEL remains unchanged (sonnet), GO_SERVED remains 0.
#
# The bridge.sh contains the complete test logic with stubbed subscription-latch.sh and curl.
name: reviewer-go-failover-limited
mode: actions
source: agents/reviewer-session.sh
parts:
  - file:bridge.sh
env:
  - PROJECT=test-project
  - PR=42
