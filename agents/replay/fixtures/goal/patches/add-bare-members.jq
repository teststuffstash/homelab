# add-bare-members — the three shapes the RETIRED walk used to switch on, in ONE world: a bare
# native descendant of open goal #29 whose declared footprint classifies machine-doable (#301),
# one whose footprint is operator-lane (#302), and one with no `Touches:` line at all (#304).
# None carries `agent-fix` or `agent/queued` — filing bound them to the tree and nothing else.
#
# Under ADR-122 (1) the classification no longer matters: filing is inert, so all three produce
# the SAME nothing, which is why they collapsed from three rows into one (the row's `expect`
# column points straight at the checkpoint-due template — the equality IS the assertion).
. + [
  {
    "number": 301,
    "title": "bare tree member — machine-doable path",
    "labels": [],
    "body": "Touches: src/thing.py\n",
    "parent": { "number": 29 }
  },
  {
    "number": 302,
    "title": "bare tree member — operator-lane path",
    "labels": [],
    "body": "Touches: .github/workflows/test.yaml\n",
    "parent": { "number": 29 }
  },
  {
    "number": 304,
    "title": "bare tree member — undeclared footprint",
    "labels": [],
    "body": "No touches line at all.\n",
    "parent": { "number": 29 }
  }
]
