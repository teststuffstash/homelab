# ── bridge ── the one launcher variable the walk is handed (REPO_SLUG, pod-time in the heredoc).
# Nothing else: the walk is pure shell over `gh`, so every hop rides the PATH-shim stub and lands
# in the asserted action stream — no gh() function here, a mock would test the mock.
REPO_SLUG="teststuffstash/test-project"
