# homelab#1349 — Base: master arming guard: a declared base equal to the repo default branch
# (master) gets the same armed arm as goal/*, so Base: master lands armed exactly as no Base:
# line already does.
#
# THE CONTRACT:
#   1. Base: master → auto-merge ARMED (protected default branch).
#   2. The launcher's stdout line says which branch and that it armed.
name: base-arm-master
mode: actions
source: agents/agent-session.sh
parts:
  - file:bridge.sh
  - block:base-arm-guard
  - file:post.sh