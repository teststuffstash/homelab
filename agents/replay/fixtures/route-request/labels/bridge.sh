# ── bridge ── the launcher variables the block consumes, as a circles queued-dispatch ride:
# TASK names an issue (so the labels read fires against the recorded world), WORK_BRANCH is empty
# (fresh round — no explicit urgency; the router's urgency_map owns the verdict), and the
# stack row/chain are what the mirror serves for circles' chainless claim.
PROJECT="circles"
TASK="issue-42"
ROUND="1"
OR_SECRET=""
HARNESS="goose"
MODEL="deepseek/deepseek-v4-flash"
WORK_BRANCH=""
ORG="teststuffstash"
_srow='{"name":"circles","modelDeny":[]}'
_chain='["deepseek/deepseek-v4-flash"]'
