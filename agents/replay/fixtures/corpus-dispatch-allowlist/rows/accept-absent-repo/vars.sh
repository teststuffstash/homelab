# ACCEPT (absent repo): the ring body was `{}` — the jq read never ran, so RING_REPO is unset and
# the `:-` default resolves the target. The bridge's `unset` is what makes this genuinely absent.
RING_PREFIX="2026-09-14"
GH_TOKEN="ghp_fake1234567890abcdef"
