# ACCEPT (empty repo): the ring body carried no `repo`, so the jq read yields "" and the `:-`
# default resolves the target — the doorbell's "payload {repo, prefix} at most" contract.
RING_REPO=""
RING_PREFIX="2026-09-14"
GH_TOKEN="ghp_fake1234567890abcdef"
