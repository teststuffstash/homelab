# ── observation point ── not launcher code: the assembled /route body is the observable. The
# curl sits outside the block, so the request JSON is the contract this fixture pins.
echo "REQ: ${_req}"
