# homelab#1276 — the env card with the MCP knob present on the opencode arm. Pins that the
# two MCP-gated lines (feedback tool + version skew) ARE emitted now that the opencode arm
# attaches MCP via OPENCODE_CONFIG (#1276).
#
# CONDITION UNDER REPLAY. render_env_card() with MCP_ENDPOINT set to the oracle stack's endpoint
# and HARNESS=opencode. AGENT_BASE_IMAGE is set so the version-skew line resolves.
name: env-card-mcp-present-opencode
mode: actions
source: agents/agent-session.sh
parts:
  - block:render_env_card
  - file:pre.sh
  - file:post.sh