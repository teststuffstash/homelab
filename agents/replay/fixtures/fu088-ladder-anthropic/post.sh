# ── observation point ── not launcher code. The finished pod command is the observable: whether
# the ladder left the dispatched model alone (anthropic clear) or re-pointed it at the Go rail
# (failover) is visible in the RUN_CMD that reaches the pod.
echo "RUN_CMD: ${RUN_CMD}"
