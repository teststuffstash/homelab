# Copy to terraform.tfvars (gitignored) only if you need to override defaults. The token is NOT here
# — it comes from the GITHUB_TOKEN env var (see providers.tf). Defaults already target teststuffstash.

# org         = "teststuffstash"
# enforcement = "active"   # default is "disabled"; flip to "active" deliberately (evaluate = Enterprise-only)

# protected_repos = {
#   sleep-tracking = { required_checks = ["ci"] }
#   snore-recorder = { required_checks = ["ci"] }
# }
