# Responder week 2026-09-04 → 2026-09-11 — every responder-authored issue and comment, classified

Method: one `gh issue list --search "updated:>=2026-09-04"` per `agents/stacks.json` repo (12 repos:
sleep-iac, sleep-tracking, snore-recorder, agent-runtime, agent-coordinator, homelab,
openrouter-operator, oracle-iac, allure-behavior-snippets, oracle-fleet, circles-iac, circles), two
org-wide `gh search issues "alert-fp:"` (body + comments), then `gh issue view --json comments` on the
21 candidates. Responder login = `homelab-agents-1234[bot]` (the `homelab-agents` App; the same login
the coordinator/fixer machinery posts as — triage writes were separated from machinery writes by
marker: `alert-fp:` / `subject:` / `fix-verdict:` / the triage prose vs `<!-- agent-summary -->`,
`ARBITRATE:`, `blocked-on:`, lifecycle-belt comments). Markers per `agents/coordinator/responder-argo.yaml`:
title `🚨 <alertname>: <summary>`, body ends `alert-fp:<fp>` + `fix-verdict: fix|report-only`
(+ `self-referential: true`, `subject:<subj>`, `Touches:`, `related: #N`, `Cause: #n`, `TOOL_GAP:`),
resolve leg rewrites a `last-cleared:` body line (no comment on fix-verdict threads).

**Window shape.** Zero responder output 09-04 → 09-08 16:18Z: every triage session since 09-01 died at
launch (`claude --model claude/sonnet` unparsed, FU-127 class; PR#1522 fixed it 09-08 16:18Z —
TICK-LOG "2026-09-08 — late corpus session", incident `docs/incidents/2026-09-05-updater-node-cap-responder-dead-triage.md`).
All activity below is 09-08 17:21Z → 09-11 02:45Z (~2.5 days). Only homelab and oracle-fleet carry
any responder write; the other 10 repos have none (oracle-iac/circles/sleep-* issues in the window are
all human/coordinator-authored, no `alert-fp:`).

## homelab — NEW issues (5)

| # | date | alert / fp | title | verdict · labels · state | queued / ride | would queuing help | noise | covered elsewhere | seat-actionable |
|---|---|---|---|---|---|---|---|---|---|
| #1546 | 09-09 | AgentAttentionStanding `f321eb335c574c03` | oracle-fleet#361 footprint-held >4h | report-only, self-ref · no labels · OPEN, no human comment; 2 same-fp re-fire comments (09-10 00:02, 09-11 02:45 — SENSIBLE, own thread) | not queued (report-only) | no — issue-metadata / codeowner call, not a PR | yes, machinery alert; but the 09-11 comment is a correct, narrow ask: oracle-fleet PR#554 (`.agents/probe.md`) is BLOCKED `REVIEW_REQUIRED` on the human CODEOWNERS read (verified live now: still OPEN/BLOCKED); #361 still `agent/queued` | TICK-LOG 2026-09-05 13:00Z ("AgentAttentionStanding oracle-fleet = meta-state operator items 5–7"); meta-state OPERATOR-ONLY 1–4 (#361's hold = oracle-iac#485) — the 09-10 re-scope (#361 reopened, blocked on #344 → PR#554) is NOT in TICK-LOG/meta-state | YES now: codeowner-read oracle-fleet PR#554, then close #1546 |
| #1547 | 09-09 | AgentDispatchCronWoken `846621f69905ce77` | platform edge scans crash SIGPIPE (exit 141) | fix, self-ref · agent-fix, agent-budget/lg, agent/blocked · OPEN; re-fire comment 09-10 02:54 confirmed the line (`coordinator-scan.sh:1495` parity-assertion `\| head -1` unguarded; `.github/workflows/ci.yaml` now has TWO `grep -E.*agents/` lines) and the responder session itself opened **PR#1576** (`\| head -1 \|\| true`) | ride r2 on PR#1576 = NO-OP (haiku did not add the ADR-103 replay pin); parked `blocked-on: human` 09-10 03:18Z; PR#1576 OPEN, MERGEABLE, REVIEW_REQUIRED (codeowner-merge on `agents/**`), ci-red on the ratchet only | **yes — and it did**: correct diagnosis, mechanical one-liner, PR exists; blocked only on the replay-pin ratchet + human merge | no. ⚠ overlaps #1456 (same SIGPIPE hunt, CLOSED `agent/done` 09-08 18:23 — its fix evidently missed this site: edge scans still died 09-08 21:31/21:53 and 09-09 18:55) and FU-219 ("site unnamed") — the responder NAMED it | FU-219 (open, still says site unnamed); TICK-LOG 2026-09-05 20:41 (#1456 filed), 2026-09-06 (FU-219), 2026-09-08 mid-morning (#1456 queued); the #1547/#1576 outcome is NOT in TICK-LOG/meta-state; alert cleared 09-10 16:36Z | YES now: add the fixture pin (or ratchet disposition) to PR#1576 and codeowner-merge; update FU-219 with the named site; note #1456's fix was incomplete |
| #1557 | 09-09 | GarageQuorumMembersRestarted `72e5bfab9298980f` | false-positive on the rollout that just fixed it | fix · agent-fix · OPEN; machinery comment = currency-skip (alert resolved 09:08Z → not queued) | not queued (FU-133 leg c currency gate) | no — the seat answered it differently the same hour: PR#1556 `minReadySeconds` 300 keeps rollouts outside the 5-min window; the belt's "2 starts in 5 min" semantics are by design (TICK-LOG: "fired on the ~2-min spacing as designed → PR#1556") | yes — a proof-fire of a belt the seat had merged 30 min earlier (PR#1552), triaged as a defect | TICK-LOG 2026-09-09 morning tail (L8119); docs/garage.md belt notes (#1590) | YES now: close as covered by #1556 (optionally keep the responder's "assert NOT fire on a paced rollout" fixture idea — the #1589 rollout 09-10 18:36/18:42/18:47Z did not fire, which already proves the spacing) |
| #1580 | 09-10 | GithubWorkflowRunFailed oracle-fleet/release-corpus `ef283957225f3d70` | ARC runner evicted mid-job on wk-03 (ephemeral-storage) | fix · agent-fix, **agent/queued** · OPEN; body itself says the path is pin-only GUARDED "no worker can land this as a PR" — yet the debouncer queued it | queued, never ridden (guarded path → would be refused/red) | no — the diagnosis (eviction on wk-03, no ephemeral request) is right, the proposed fix is the one the seat ruled unworkable: "requests compare to allocatable, not to the disk the image store took, and scale sets have no labels" → real fix = a NEW scale set `homelab-ephemeral-large` (#1582) + oracle switching `runs-on` | partial — the same handoff (`20260910-0731`) reached the seat at 07:31Z; the responder's 08:0x issue duplicates the handoff | TICK-LOG 2026-09-10 early ("Oracle handoff 07:31 — runner evicted on wk-03"); meta-state PICKUP (1)–(2) (#1581 belts, #1582 set, oracle must switch runs-on — unverified until Tuesday's 07:17Z run) | YES now: de-queue (drop `agent/queued`), close as covered by #1581/#1582, keep the Tuesday verification in meta-state |
| #1584 | 09-10 | PodEvicted `77aa20350f797717` | arc-runners runner evicted — same event as #1580 | fix, self-ref, `Cause: #1580` · agent-fix · OPEN, no comments | not queued (child of #1580) | no (same as #1580) | yes — duplicate by construction (the #149 one-subject rule forces a second issue for the same event) AND a proof-fire: `PodEvicted` is the seat's new belt (#1581) that "FIRED live 08:03Z" — the responder filed an issue on the detector's replay | TICK-LOG 2026-09-10 early (#1581 `PodEvicted` … FIRED live 08:03Z) | YES now: close with #1580 |

## homelab — responder COMMENTS on existing threads (26) + oracle-fleet (1)

Kind key: SENSIBLE = same problem as the thread's title/problem statement; GRAFT = different problem
attached because the computed `subject:` collapsed onto the thread (the reporter workload —
kube-state-metrics / pushgateway / node-exporter / the cilium DaemonSet — not the failing object).

| thread | comment(s) | alert / fp | kind | verdict | diagnosis quality / speculation | noise | covered elsewhere | seat-actionable |
|---|---|---|---|---|---|---|---|---|
| #100 LonghornDiskAlmostFull thinkcentre/optane0 (subject `workload:longhorn-system/longhorn-manager`, OPEN) | 09-09 01:11 | LonghornNodeOverProvisioned wk-metal-04 179.8% `8f19cc5a34c74588` | GRAFT (3rd node, 3rd alert class, says so itself) | report-only, self-ref | evidence good (rebuild replica of `data-garage-0` double-booking 161 GB; bulk tier ≥100% committed on both members). Speculation: *"`docs/storage-ledger.md`'s 2026-09-07-evening zone-rotation entry … is very likely the origin of this exact rebuild"* — plausible, unverified | yes — transient (cleared 01:36Z); moot since 09-09 evening (garage-0 → 7600p `intel1`, `sata500` → `slow-bulk`, TICK-LOG 2026-09-09 20:00Z) | TICK-LOG 2026-09-09 early ("#500/#100 (nix-cache/optane) — none garage" — dismissed; it was in fact garage's rebuild) + 2026-09-09 evening (wk-metal-04 demote) | nothing; thread stays a #149-class graft magnet (documented in responder-argo.yaml header) |
| #1013 GarageTableEmpty is-zero FP (subject `workload:garage/garage-0`, OPEN) | 09-09 02:03 | GarageDiskFillingUp garage-0 metadata 88.6% `f3b2c0521ab67ce5` | GRAFT (same pod, different problem: capacity vs a rule FP) | report-only | mechanism partly right (LMDB never shrinks; rotation loop unarmed = FU-137 next) but missed the real cause the seat found 3 h later (born bloated by native-resync seeding, not "recurring") | yes — resolved by the seat the same morning: rotation loop built PR#1549, first run 08:45Z garage-0 27.83 → 4.68 GB | TICK-LOG 2026-09-09 early + morning; FU-137 pointer; garage.md §Metadata reclamation | nothing (done). #1013 itself (is-zero FP, fix scoped in #977) is a separate open item |
| #103 NodeSystemSaturation wk-01 (subject `…node-exporter` — THE #149 graft thread) | 09-10 01:00 | NodeMemoryMajorPagesFaults m70s 3688/s `ca4004fb1c4f5028` | GRAFT (different node, different alert; the marker itself is the 08-08 graft) | report-only | Speculation: *"m70s, being the newest node with the most free capacity, is where that pre-existing gap [cross-namespace TopologySpread] is surfacing first — same mechanism, new node."* Likely WRONG: m70s hosts **garage-1**; the seat's 09-10 design read attributes major faults on zone nodes to the LMDB page cache ("16 GB for the LMDB page cache (X240 at 8 GB: 200–1,800 major faults/s)"), and the 09-09 evening seat had just moved oracle's ERT delta onto m70s (oracle-iac#711 "m70s freed for the delta") — a heavy Garage writer on the zone node | mostly — no action taken, alert cleared 01:38Z | partial: TICK-LOG 2026-09-10 afternoon (zone-node envelope, page-cache faults) + memory fleet-roles ("no rides on zone nodes"); not by node name; no FU for the cross-namespace spread gap (the comment grepped, negative — consistent) | needs light digging: one PromQL (`rate(node_vmstat_pgmajfault{instance="192.168.2.56:9100"}[5m])` vs garage-1 LMDB size / the delta job's schedule) settles LMDB-vs-burst; then it is the fleet-roles direction, not a spread constraint |
| #114 AgentReviewLoop renovate-approve (subject `workload:monitoring/github-exporter`, OPEN) | 09-08 17:21 | AgentErrorFlagged oracle-fleet#521 `1f1925a3c10293bb` | GRAFT (self-declared: "independent of true root cause") | report-only, self-ref | correct pointer: root cause = oracle-fleet#523 (`scripts/e2e-serve.sh` state leak), filed by the fleet's own ci-red ride 1 min before the alert | yes — pure cross-reference; the alert was the symptom of a stack fault already on file | TICK-LOG 2026-09-08 late corpus (L7961: "the first responder investigation in ≥7 days"; L8043: #523 latched agent/error on 4 green PRs, cleared by hand, #1539 filed + queued) | nothing (done) |
| #153 PodSigkilled Prometheus no memory limit (subject `…prometheus-0`, OPEN, label `agent/review` stale) | 09-08 20:53 | PrometheusHighQueryLoad 19/20 slots `bcb2c42c40d38506` | GRAFT-lite (same pod, different problem; the OOM half was fixed by PR#171 + the 8Gi limit) | **fix** — `Touches: argocd/platform/values/kube-prometheus-stack.yaml`, codeowner-merge | verified live (`prometheus_engine_queries` 19/20, `query.max-concurrency` at binary default 20, 65 groups/358 rules/128 targets); proposes `prometheusSpec.query.maxConcurrency: 40`. Not speculative | no — real capacity ceiling | **NOT covered**: no TICK-LOG/meta-state/FU hit for `PrometheusHighQueryLoad` / `maxConcurrency`; the verdict sits in a COMMENT so the dispatch half never read it (it reads the issue BODY — ADR-122 (2)); no ride | **would queuing have helped: YES** (one-line values bump). Needs light digging: is it recurring (the garage-upstream dashboard #1573 + SLO rules #1561/#1588 landed after the fire and add query load), then bump and re-evaluate. Own issue warranted (the thread is the OOM record) |
| #241 RegistryMirrorDanglingLinks (subject `workload:monitoring/prometheus-pushgateway` = every pushgateway-served gauge, OPEN) | 09-10 06:13 · 09-11 02:18 | RegistryBucketCommitHeadroomLow `05a2574b1c2274df` · GarageBucketQuotaNear ert-snapshots 84% `34e3f94625377353` | GRAFT ×2 (different subsystems entirely — first-party registry quota; an oracle-iac bucket) | report-only ×2 | both accurate reads of the seat's own NEW belts (#1577) on their proof-fires; first one correctly names "a newly-shipped alert seeing pre-existing headroom for the first time"; ownership split (ADR-085) stated right | yes ×2 — the seat merged fix #1578 (32Gi → 48Gi) against the firing alert the same hour; ert-snapshots/allure-reports are oracle-iac's, told in the handoff result | TICK-LOG 2026-09-10 early ("Belt replay, live (06:10Z)"); meta-state PICKUP (2) "Firing by design at wind-down: GarageBucketQuotaNear on ert-snapshots 84 % and allure-reports 89 % — both oracle-iac's (told)"; FU-203, FU-228 | nothing. ⚠ #241 is now the org-wide search hit for every pushgateway alert (Garage bucket gauges, registry, mirror, agent metrics) — a structural graft magnet |
| #500 AgentRunPhaseSlow nix-cache (subject `alert:AgentRunPhaseSlow`, reopened by the responder; was CLOSED 08-26; label `agent-fix` from its prior life) | 09-09 00:12 | same fp `5cdda578ba513dcd` re-fire | SENSIBLE (same fp) | report-only, self-ref | good: verified PR#512's fix intact (20Gi, 66% used, `max_size=16g` + resolver both live); n=3 window (59 s/137 s/112 s vs baseline p50 46 s) = thin-sample variance, the weakness RasmusSoot flagged 08-23. Circumstantial only: *"live node churn … TaintManagerEviction … ~3min before startsAt. This is circumstantial, not confirmed"* (flagged as such) | yes — the RULE is the noise (n=3 current window); cleared 12:54Z | TICK-LOG 2026-09-09 early ("#500/#100 — none garage"); meta-state (4) older DEFERRED-STUCK note (09-05) | YES now: close #500 again; the 5-minute fix is a `count(...) >= N` sample floor on the `AgentRunPhaseSlow` expr (no FU found for it — grep `AgentRunPhaseSlow` in the open tracker is negative) |
| #542 KubeDaemonSetRolloutStuck cilium / wk-03 (subject `…kube-state-metrics`, OPEN) | 09-09 17:10 | same alert, node thinkcentre `7a89cabb930c2070` | SENSIBLE-ish (same DaemonSet, same alert; different node) | report-only, self-ref | correct — found the cordon-before-unreachable ordering and hypothesis 3 (the operator's `node-maintenance down thinkcentre` after the PR#1564 drain-policy fix) is exactly what happened | yes — planned maintenance (TICK-LOG 2026-09-09 evening: three live drive windows on thinkcentre) | TICK-LOG 2026-09-09 evening "the drive windows" | nothing. Structural: `scripts/node-maintenance.sh` creates NO Alertmanager silence (grep `silence` negative) → every window costs responder sessions (see totals) |
| #811 KubeStatefulSetReplicasMismatch loki — Garage quota (subject `workload:loki/kube-prometheus-stack-kube-state-metrics`, OPEN) | 09-09 17:03 · 09-10 00:19 · 09-10 00:24 | KubeDaemonSetRolloutStuck alloy `97db97188377ed7c` · KubeStatefulSetReplicasMismatch loki `5ff978ed0c31381e` · KubePodCrashLooping loki-0 `4fb0c64cb42d7fd3` | c1 GRAFT (alloy on thinkcentre — maintenance); c2/c3 GRAFT-by-cause (loki-0 crashlooping again, but WAL corruption, not the quota) | report-only ×3, c2/c3 carry `REMEDIATION-WOULD: delete /loki/tsdb-index/wal … chunks/index rebuild from Garage S3` | c1 correct (maintenance, cites commit 3e51f67). **c2 is the correct Loki diagnosis, 5 h before the seat found "Loki dead 11.5 h, nobody read the alert" at ~05:1xZ** — exact segment, cause class (unclean termination = hp-01's 17:17Z plug cycle), remedy (what the seat then did from a helper pod). c3 adds nothing (same pod UID) | c1 yes (maintenance); c2 NO — genuine, actionable, and unread; c3 yes (second rule on the same condition) | TICK-LOG 2026-09-10 early ("Loki dead 11.5 h") + incident doc + runbook §Power-loss; **meta-state PICKUP (4) asks "why no responder issue for an 11-h KubePodCrashLooping — dedup against #811 is the first suspect"** → confirmed here: the triage landed as comments on #811 via the collapsed kube-state-metrics subject, not as an issue; AND it came 6 h after `startsAt` (18:15Z → 00:19Z). Hypothesis for the 6 h: the FU-149 daily latch (`RESPONDER_DAILY_MAX=12`) was spent on the thinkcentre storm (≥8 sessions 17:00–17:20Z) + hp-01; the burst of triages at 00:02/00:15/00:19/00:24/01:00Z is a 00:00Z reset signature. Verify: `ResponderTriageBudgetExhausted` / the pushgateway budget gauge for 09-09 | YES now: answer meta-state (4) with this; then decide the two structural fixes (silence during maintenance windows; budget spent on planned noise) — both un-tracked (grep negative) |
| #857 PodSigkilled cilium-agent OOMController (subject `workload:kube-system/cilium`, OPEN, FU-155's symptom thread) | 09-09 17:00 · 17:03 · 17:08 · 17:16 · 17:20 · 09-10 00:15 · 09-11 01:10 | CiliumUnreachableNodes `228182143727a74e` · KubeletInstanceUnreachable `9bbb71f9cedbb3eb` · KubePodNotReady alloy-7nj5k `c0bb40a7032f4b58` · PodSigkilled engine-image `f96312bf19f394c7` · PodSigkilled runner-image-prepull `38930a1e9a79276a` · ContainerMemoryNearLimit cilium-agent wk-metal-04 85% `37bfa7be524507bc` · same on wk-03 86% `b8c024a1fd5945a7` | c1 GRAFT (subject match, different mechanism); c2–c5 = "related by LINK" cross-reference comments WITHOUT a subject marker (the #149-compliant form) — 4 comments in 17 min on one thread; c6/c7 GRAFT-lite (same container, but limit creep ≠ shared-fate kill) | report-only ×7 | c1–c5 all correct (thinkcentre cordon→drain→shutdown = the seat's windows; c4 even caught the second cycle 27 min later = the seat's second window). c6/c7: solid evidence (cilium-agent working set 432Mi/512Mi flat on wk-metal-04; 85% on the 16Gi VM wk-03 — "weakens the kata-fit framing"), defers to the FU-155 tune-vs-accept ruling | c1–c5 yes (maintenance storm, 5 sessions); c6/c7 no — new evidence on a live operator question | c1–c5: TICK-LOG 2026-09-09 evening. c6/c7: partially — FU-155 "cilium-agent's residual exposure … folds into the same ruling"; meta-state (7)–(8) CPU/request-tax discussion ranks "cilium-agent Burstable" as the cheapest lever (CPU); FU-224/PR#1519 raised cilium CPU limit to 500m 09-08 — memory 512Mi untouched; `ContainerMemoryNearLimit` itself: no TICK-LOG/FU hit | needs light digging: fleet-wide `container_memory_working_set_bytes{container="cilium-agent"}` vs 512Mi over 14 d → decide 512Mi → 768Mi (or Burstable memory) under the meta-state (7) lever discussion; a worker COULD do the `tofu/cilium.tf` bump but the number is the operator's kata-fit call |
| #882 KubeNodeNotReady wk-01 (subject `…kube-state-metrics`, OPEN) | 09-09 17:05 | KubeNodeUnreachable thinkcentre `703f59d6746abc8f` | GRAFT (different node) | report-only | Speculation: *"most consistent with FU-032's known thinkcentre-specific bad-cable class recurring, though I cannot rule out an unlogged planned cordon/power action"* — the second was true, the first (cable) wrong; it did name the cordon ordering | yes (maintenance) | TICK-LOG 2026-09-09 evening | nothing |
| #884 EndpointProbeFailing specs.oracle — Garage wipe (subject `ns:monitoring`, OPEN, 2 human replies) | 09-09 01:39 · 09-10 08:30 | same fp `1763380031d0c94e` re-fire · GarageClusterFlapping garage-2 `49b03e57e5d0cc1f` | c1 SENSIBLE (same fp); c2 GRAFT (new alert class, ns:monitoring collapse) | report-only ×2 | c1 "transient probe blip, no fix surface" — seat corrected 05:55: the blip had a cause (probe landing on garage-0's hourly SA400 stall, #903). c2: node right, device wrong — Speculation: *"specific to the `sdc` spindle backing garage's local-xfs volume(s)"* and *"Plausibly downstream of the active rf=3 rebalancing work … heavier resync fan-out onto peers during/after a rotation"* — seat corrected 08:38: `sdc` is the `data-garage-2` Longhorn iSCSI virtual disk; what saturated was CPU (Longhorn instance-manager 1.6 of 4 cores) under oracle's corpus release with garage-2 as S3 coordinator | c1 partly (cause existed); c2 no — real belt fire, wrong mechanism, seat-corrected in-thread | TICK-LOG 2026-09-09 early (#884 given its cause) + 2026-09-10 early ("#884 re-fire 08:25Z … the responder's read corrected"); PRs #1585 (arc-large prefers the laptop without a zone), #1589 (Garage CPU 500m); FU-137 pointer (no storage zone on a 4-core laptop) | nothing left (done; cleared 14:28Z). Consider closing #884 — its original problem (08-24 wipe) is ADR-114/rf=3 done; it now serves as the `ns:monitoring` graft magnet |
| #903 KubePersistentVolumeFillingUp meta-garage-0 (subject `pvc:garage/meta-garage-0`, OPEN, `agent/blocked`, 1 human reply) | 09-09 01:08 | same PVC, new fp `7bb1691990b0a38c` | SENSIBLE (same PVC, same alert) | **fix** | WRONG on the deciding fact — Speculation: *"Chronic metadata regrowth from sustained overwrite-PUT churn … The 30Gi sizing from #903 bought ~2 weeks of headroom at this churn rate, not a durable fix"* + "(a) another PVC expansion … or (b) fix the churn at the source via oracle-fleet#499". Seat 05:55: the PVC was re-cut 09-07 (PR#1498 step 8) and born bloated by native-resync seeding (27.9 GB vs 6.3 GB snapshot-seeded); neither (a) nor (b) is the answer → snapshot-seeded rotation (PR#1548 doc fix + PR#1549 loop; run 08:45Z → 4.68 GB) | no — but had it been queued the worker would have expanded a PVC for nothing | TICK-LOG 2026-09-09 early + morning; FU-137; garage.md §Metadata reclamation (corrected) | YES now: close #903 (cleared 08:51Z; garage-0 rotated again onto the 7600p 09-09/10; loop live) |
| oracle-fleet#228 GithubWorkflowRunFailed oracle-fleet/CI e2e race (subject `workflow:oracle-fleet/CI`, OPEN, 1 human reply) | 09-09 07:39 | run #1015 evidence-publish quorum failure `6542f2e3831b1769` (no `fix-verdict:` line — prose "transient, self-resolved") | SENSIBLE (same workflow, different failure) | none / transient | **correct and valuable**: ArgoCD `garage` sync 07:22:34Z → all three pods restarted within 31 s → quorum-of-2 delete failed at 07:22:50Z; the "PDB/maxUnavailable" suggestion was the wrong lever (STS rollouts wait on readiness, not PDBs) but the finding drove the seat's detector-first build (PR#1552 `GarageS3ServerErrors` + `GarageQuorumMembersRestarted`) and fix (PR#1555 readiness probe + `minReadySeconds`, PR#1556 300 s) — seat replied on-thread 09:45 | no — the one triage this week that the seat built on directly (and the origin of the "detection before fix" ruling) | TICK-LOG 2026-09-09 morning ("Operator ruling … Applied to oracle-fleet#228's rollout-quorum-loss comment"); memory `detection-before-fix.md`; seat card §Safety | nothing (done). #228 stays open as oracle's own CI-race record |

Excluded (same login, not responder): #887 and #1467 timelines (coordinator agent-summary / ARBITRATE / lifecycle belts), the `currency-skip` on #1557 and the agent-summary + park on #1547 (dispatch half).

## Totals

- **Writes:** 5 new issues + 28 triage comments = 33 responder writes on 19 threads, 26 distinct fingerprints (3 same-fp re-fires on #1546 ×2 / #1547 ×1). All homelab except one comment on oracle-fleet#228. Ten of twelve repos: zero responder activity.
- **Sensible vs graft (28 comments):** 9 SENSIBLE (#1546 ×2, #1547, #1580's `related:` link, #500, #884 c1, #903, oracle-fleet#228, #542) vs **19 GRAFT** (#100, #1013, #103, #114, #153, #241 ×2, #811 ×3, #857 ×7, #882, #884 c2). Every graft is the *collapsed-subject* class: the computed subject is the REPORTER (kube-state-metrics → #811/#882/#542; pushgateway → #241; node-exporter → #103; the cilium DS → #857; `ns:monitoring` → #884), so the #149 rule is followed to the letter and still lands unrelated problems on five magnet threads. No FU tracks this (grep `collapsed subject|computed subject|reporter` negative); the #149 header in responder-argo.yaml describes the failure but the subject key still does it.
- **Verdicts:** issues — 4 `fix` (#1547, #1557, #1580, #1584) / 1 report-only (#1546). Comments — 2 `fix` (#153, #903) / 25 report-only / 1 no line (#228). **Queued:** 1 (#1580 — on a pin-only-GUARDED path its own body says no worker can land; never ridden). **Rides:** 1 (#1547 → PR#1576, r2 no-op, parked `blocked-on: human`). **Currency-skipped:** 1 (#1557).
- **Would queuing have helped:** 2 — #1547 (yes, and the responder session itself opened the PR; blocked on the ADR-103 replay pin + codeowner merge) and #153 (a one-line `maxConcurrency` values bump; its `fix` verdict sits in a comment the dispatch half never reads). The other `fix` verdicts would have shipped the wrong fix (#1580/#1584 ephemeral request on the general set; #903 PVC expansion) or a redundant one (#1557).
- **Noise:** 24 of 33 writes. By class: planned-maintenance storm on thinkcentre 09-09 17:00–17:20Z = **9 writes / ≥8 sessions** (#542, #882, #811 c1, #857 c1–c5, and the #100/#1013-adjacent cordon reads) with no silence issued by `node-maintenance.sh`; the seat's own belt proof-fires triaged as defects = 4 (#1557, #1584, #241 ×2); known/cleared/self-referential machinery = 7 (#1546 ×3, #500, #114, #100, #1013); speculative-and-wrong = 4 (#882 cable, #884 c2 spindle/resync, #903 churn regrowth, #103 job-burst). **Genuine signal = 6**: oracle-fleet#228 (quorum loss → the detector-first build), #811 c2 (Loki WAL, 5 h ahead of the seat, unread), #1547 (SIGPIPE site named — #1456's fix had missed it), #153 (query concurrency), #857 c6/c7 (cilium-agent memory creep evidence), #1580 (right cause, wrong fix).
- **Covered in TICK-LOG / meta-state / FU:** 16 of 19 threads. **Not covered:** #153 PrometheusHighQueryLoad (nowhere); #857 c6/c7 ContainerMemoryNearLimit (only obliquely via FU-155 + meta-state (7)); #103 m70s major faults (only via the 09-10 zone-node envelope read, not by node). Two facts the records have WRONG or open: meta-state (4) "no responder issue for the 11-h Loki crashloop" → it exists as #811 c2/c3 (00:19Z), delayed ~6 h (daily-budget reset signature at 00:00Z — hypothesis, verify); FU-219 "site unnamed" → named by #1547 (`coordinator-scan.sh:1495`).
- **Seat-actionable now vs needs digging:** now = 11 (close/de-queue #1557, #1580, #1584, #903, #500; merge PR#1576 + update FU-219; codeowner-read oracle-fleet PR#554 → close #1546; answer meta-state (4) from #811; nothing further on #114, #241, #542, #882, #884, #1013, #100, #228). Digging = 3 (#153 recurrence + bump; #857 c6/c7 fleet-wide cilium-agent memory read → sizing call; #103 one PromQL: LMDB page cache vs job burst on m70s).

## Structural findings (for the operator's pass)

1. **The subject key is the reporter, not the failing object** — 19/28 comments are grafts onto five magnet threads (#811, #882, #542 via kube-state-metrics; #241 via pushgateway; #103 via node-exporter; #857 via the cilium DS; #884 via `ns:monitoring`). Each comment dutifully says "same computed-subject quirk". Untracked.
2. **Maintenance windows are invisible to the responder** — no silence from `node-maintenance.sh`; 09-09's three thinkcentre windows cost ≥8 sessions of the 12/day budget and (hypothesis) starved the hp-01 outage (17:17Z) and Loki death (17:55Z) of a same-day triage until the 00:00Z reset. Untracked (grep `silence`, `RESPONDER_DAILY` negative in the tracker; FU-149 archived).
3. **Detector-first produces proof-fires the responder triages as defects** (#1557, #1584, #241 ×2) — 4 sessions on belts the seat had merged the same hour.
4. **`fix` verdicts in comments are inert** (#153) — the dispatch half reads the issue body only (ADR-122 (2)); a `fix` on a grafted thread can never queue.
5. **#1580 was queued on a path its own body declares un-landable** (pin-only GUARDED) — the debouncer does not read the `classify_touches` sentence.
6. **#1456 closed `agent/done` 09-08 without fixing the SIGPIPE** — the responder's #1547/PR#1576 found the actual site the next day; FU-219 still says "site unnamed".

## OPEN responder issues (19) — what to do / where the context is

| issue | what to do | context |
|---|---|---|
| homelab#1546 AgentAttentionStanding (oracle-fleet#361) | codeowner-read oracle-fleet PR#554 (`.agents/probe.md`, bot-approved, BLOCKED REVIEW_REQUIRED — verified now), then close | #1546 09-11 comment; #361 reopened 09-10 12:23 blocked on #344 → PR#554 |
| homelab#1547 AgentDispatchCronWoken SIGPIPE | add the ADR-103 replay pin (sentinel + fixture per the round-2 ARBITRATE on PR#1576) or a ratchet disposition, codeowner-merge PR#1576; update FU-219 with the named site; note #1456's fix was incomplete | PR#1576 comments 03:07/03:18Z; FU-219; TICK-LOG 2026-09-06 |
| homelab#1557 GarageQuorumMembersRestarted FP | close as covered by PR#1556 (`minReadySeconds` 300; the 09-10 18:36Z rollout did not fire) | TICK-LOG 2026-09-09 morning tail |
| homelab#1580 GithubWorkflowRunFailed release-corpus | drop `agent/queued`, close as covered by #1581/#1582; keep Tuesday's 07:17Z run verification in meta-state (oracle must switch `runs-on`) | TICK-LOG 2026-09-10 early "Oracle handoff 07:31"; meta-state PICKUP (1) |
| homelab#1584 PodEvicted (Cause: #1580) | close with #1580 | same |
| homelab#903 KubePersistentVolumeFillingUp meta-garage-0 | close — rotated twice (08:45Z loop run → 4.68 GB; 09-09/10 onto the 7600p); loop live | TICK-LOG 2026-09-09 early/morning/20:00Z; FU-137 |
| homelab#500 AgentRunPhaseSlow (reopened by responder) | close; 5-minute fix = sample floor on the rule (n=3 windows fire) | #500 09-09 comment; RasmusSoot 08-23 comment on the same weakness |
| homelab#153 PodSigkilled Prometheus → PrometheusHighQueryLoad | file/own: check recurrence since the 09-09/10 dashboards+rules landed, bump `prometheusSpec.query.maxConcurrency` (20 → 40) in `argocd/platform/values/kube-prometheus-stack.yaml` | #153 09-08 comment (evidence complete); nothing in TICK-LOG/FU |
| homelab#857 PodSigkilled cilium-agent (FU-155 thread) | read c6/c7: fleet-wide cilium-agent working set vs 512Mi → decide the memory limit under the meta-state (7) lever discussion; the thinkcentre-storm comments c1–c5 need nothing | FU-155; meta-state (7)–(8); FU-224/PR#1519 (CPU only) |
| homelab#811 loki crashlooping (Garage quota) | answer meta-state (4): the Loki WAL triage exists here (c2, 00:19Z), delayed ~6 h — verify the daily-budget hypothesis; original quota problem long fixed (16Gi) → consider closing #811 so it stops being the kube-state-metrics magnet | TICK-LOG 2026-09-10 early; incident doc; runbook §Power-loss |
| homelab#884 EndpointProbeFailing specs.oracle (Garage wipe) | nothing pending (#1585/#1589 landed, cleared 14:28Z); consider closing — its 08-24 problem is ADR-114 done and it is now the `ns:monitoring` magnet | TICK-LOG 2026-09-10 early "#884 re-fire" |
| homelab#103 NodeSystemSaturation wk-01 (node-exporter magnet) | one PromQL on m70s major faults vs garage-1 LMDB / the ERT delta schedule; then it is fleet-roles direction (no rides on zone nodes), not a spread constraint | TICK-LOG 2026-09-10 afternoon (zone-node envelope); memory fleet-roles-direction |
| homelab#100 LonghornDiskAlmostFull optane0 (longhorn-manager magnet) | nothing (transient rebuild; wk-metal-04 re-disked 09-09) | TICK-LOG 2026-09-09 evening |
| homelab#1013 GarageTableEmpty is-zero FP | nothing from this week's comment (rotation loop live); the original FP fix is scoped in #977 | FU-137; #977 |
| homelab#114 AgentReviewLoop (github-exporter magnet) | nothing (oracle-fleet#523 owns it; #1539 queued) | TICK-LOG 2026-09-08 late |
| homelab#241 RegistryMirrorDanglingLinks (pushgateway magnet) | nothing (#1578 landed; ert-snapshots/allure-reports are oracle-iac's, told) | TICK-LOG 2026-09-10 early; FU-203, FU-228 |
| homelab#542 KubeDaemonSetRolloutStuck (kube-state-metrics magnet) | nothing (maintenance); structural: silence during `node-maintenance` windows | TICK-LOG 2026-09-09 evening |
| homelab#882 KubeNodeNotReady wk-01 (kube-state-metrics magnet) | nothing (maintenance) | same |
| oracle-fleet#228 GithubWorkflowRunFailed CI e2e | nothing (seat replied 09:45; belts + fix landed); oracle's own record | TICK-LOG 2026-09-09 morning |
