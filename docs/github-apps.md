# GitHub App installations

_Placeholder — run `devbox run github-apps` **outside the jail** with an admin PAT (`admin:org`) to
regenerate this as a live app×repo matrix. App installs are click-only (not tofu), so this file is the
only source of truth for "which App is on which repo" — regenerate after any install/removal._

| App (scope) | … |
|---|---|
| _(run the generator)_ | |
